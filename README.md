1. crée le fichier known_host: ```ssh-keyscan IP_SERVEUR``` et le mettre dans le dossier ssh_credentials
2. crée ses clefs privé/publique dans le dossier ssh_credentials et les mettres sur le serveur.
3. 